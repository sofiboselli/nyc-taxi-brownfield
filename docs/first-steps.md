# First Steps — nyc-taxi-brownfield

*Generated by `/de-init` on 2026-09-15T16:00+00:00. Top-priority work to tackle as you onboard onto this project.*

Read [`docs/project-profile.md`](./project-profile.md) for the full inventory of catalogs, jobs, and governance posture. Re-run `/de-audit --sync` weekly to keep both files fresh.

---

## Top 3 priorities

### 1. [HIGH] drift — 1 job(s) have never run — likely orphaned test deploys or half-finished work.

- **Fix**: Delete the job if it was a one-off test, or trigger a first run to confirm it works. Jobs with no runs are invisible to monitoring.
- *Evidence*: Jobs: `[prod] Gold checksum (fox_dev)` (id=683340637414104)

### 2. [HIGH] governance — Only 0% of tables have a COMMENT — readers have to guess what each table is for.

- **Fix**: Add `COMMENT 'one-line purpose'` on each table. Prioritise Bronze landing tables (source of all downstream questions) and Gold marts (consumer-facing).
- *Evidence*: Scanned: `dev_ai_kit_demo_brownfield`

### 3. [MEDIUM] governance — Only 0% of columns have a COMMENT — Gold-layer consumers can't self-serve without asking the team.

- **Fix**: Add `COMMENT` per column in CREATE TABLE / ALTER TABLE ADD COLUMNS. The `qubika-unity-catalog-governance` skill has a copy-pasteable template.

---

## Where to look next

- [`docs/project-profile.md`](./project-profile.md) — full inventory and governance/drift/usage sections
- *(no `CLAUDE.md` yet — the kit's `~/.qubika-data-ai-dev/CLAUDE.md` provides default behavior; copy it as a starting point if this project needs overrides)*
- `qubika-jira-workflow` skill — how to pick up your first ticket
- `qubika-cost-investigator` agent — deep-dive on individual statement_ids surfaced by the usage section

## When to re-run

- `/de-audit --sync` — weekly refresh of `docs/project-profile.md` (this file is regenerated alongside it)
- `/de-audit --catalogs <name> --deep` — quarterly for the full deep-scan + cost analysis
- `/de-init` again — only if the project layout fundamentally changes (rare); the recurring refresh is `/de-audit`
